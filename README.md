# B3@$T
Instead of installing black arch which is out dated and face package conflicts AKA Civil War inside blackarch
## You can use this scripts to install the tools you need
### Steps
    chmod +x *
    ./security-beast.sh

### then if failed run the last file with aur